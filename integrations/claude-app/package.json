{
  "name": "@orbit-runtime/claude-orbit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Orbit as an MCP server Claude can be pointed at, over stdio.",
  "bin": { "claude-orbit": "lib/main.js" },
  "main": "lib/main.js",
  "files": ["lib/**/*.js", "lib/**/*.d.ts", "README.md"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@orbit-runtime/integration-core": "file:../../integration-core"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^6.0.3"
  }
}
